<section>
  <div class="row">
    <div class="col-lg-7">
      <div id="carouselExampleFade" class="carousel slide carousel-fade" data-bs-ride="carousel">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <img src="camp1" class="d-block w-100" alt="...">
          </div>
          <div class="carousel-item">
            <img src="camp2" class="d-block w-100" alt="...">
          </div>
          <div class="carousel-item">
            <img src="camp3" class="d-block w-100" alt="...">
          </div>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleFade" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleFade" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>

    </div>

    <div class="mt-2 col-lg-5 d-flex flex-column min-vh-100">
      <div class="rounded-3 w-100 ">
        <h2 class="mb-4 w-100">Log in</h2>
            <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="mb-5 ">
                <div class="form-inputs ">
                  <%= f.input :email,
                              required: false,
                              autofocus: true,
                              input_html: { autocomplete: "email" } %>
                  <%= f.input :password,
                              required: false,
                              input_html: { autocomplete: "current-password" } %>
                  <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
                </div>

                <div class="form-actions w-100 btn btn-outline-secondary btn-sm mb-2">
                  <%= f.button :submit, "Sign in" %>
                </div>
              </div>
            <% end %>

            <div class="text-muted text-decoration-none">
              <%= render "devise/shared/links" %>
            </div>
      </div>
    </div>
  </div>
</section>
